[project]
name = "magicsquare-1004"
version = "0.1.0"
description = "MagicSquare 4×4 (MagicSquare_1004) — ECB + Dual-Track TDD"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
